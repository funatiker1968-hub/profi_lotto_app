pluginManagement {
    // Flutter SDK-Pfad für Codemagic
    def flutterSdkPath = System.getenv("FLUTTER_ROOT")
    if (flutterSdkPath == null) {
        flutterSdkPath = "/usr/local/flutter" // Codemagic Default
    }
    includeBuild("${flutterSdkPath}")

    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    id "dev.flutter.flutter-gradle-plugin" apply false
}

include(":app")
